name: Build XR1710G Firmware

on:
  workflow_dispatch:
    inputs:
      device:
        description: '目标设备'
        required: true
        default: 'gemtek_xr1710g-ubi'
        type: choice
        options:
          - gemtek_xr1710g-ubi
          - gemtek_w1700k-ubi
          - gemtek_xg2010g-ubi
      threads:
        description: '编译线程数 (留空则自动)'
        required: false
        default: ''
        type: string
  schedule:
    - cron: '0 0 */14 * *'

env:
  DEVICE: gemtek_xr1710g-ubi

jobs:
  build-xr1710g:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (inputs.device == 'gemtek_xr1710g-ubi' || inputs.device == ''))
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: true

      - name: Setup build environment
        run: |
          sudo apt update
          sudo apt install -y build-essential clang gcc g++ binutils bzip2 gawk gettext \
            git libncurses-dev libssl-dev python3 python3-setuptools rsync unzip \
            wget xsltproc zlib1g-dev file which perl sed make

      - name: Install feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Configure
        run: |
          # 使用 .config 或创建默认
          if [ ! -f .config ]; then
            make defconfig
          fi
          # 确保设备配置正确
          if ! grep -q "DEVICE_gemtek_xr1710g-ubi=y" .config; then
            echo "CONFIG_TARGET_airoha_an7581_DEVICE_gemtek_xr1710g-ubi=y" >> .config
          fi
          make defconfig

      - name: Download sources
        run: make download -j$(nproc)

      - name: Build XR1710G
        run: |
          THREADS="${THREADS:-$(nproc)}"
          echo "Using $THREADS threads"
          make -j$THREADS image DEVICE=gemtek_xr1710g-ubi 2>&1 | tee build.log

      - name: Show artifacts
        run: |
          echo "=== XR1710G Firmware ==="
          ls -lh bin/targets/airoha/an7581/*xr1710g* 2>/dev/null || echo "No XR1710G artifacts"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xr1710g-firmware
          path: |
            bin/targets/airoha/an7581/*xr1710g*
            bin/targets/airoha/an7581/sha256sums
          retention-days: 30

  build-w1700k:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && inputs.device == 'gemtek_w1700k-ubi'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: true

      - name: Setup build environment
        run: |
          sudo apt update
          sudo apt install -y build-essential clang gcc g++ binutils bzip2 gawk gettext \
            git libncurses-dev libssl-dev python3 python3-setuptools rsync unzip \
            wget xsltproc zlib1g-dev file which perl sed make

      - name: Install feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Configure
        run: |
          if [ ! -f .config ]; then
            make defconfig
          fi
          if ! grep -q "DEVICE_gemtek_w1700k-ubi=y" .config; then
            echo "CONFIG_TARGET_airoha_an7581_DEVICE_gemtek_w1700k-ubi=y" >> .config
          fi
          make defconfig

      - name: Download sources
        run: make download -j$(nproc)

      - name: Build W1700K
        run: |
          THREADS="${THREADS:-$(nproc)}"
          echo "Using $THREADS threads"
          make -j$THREADS image DEVICE=gemtek_w1700k-ubi 2>&1 | tee build.log

      - name: Show artifacts
        run: |
          echo "=== W1700K Firmware ==="
          ls -lh bin/targets/airoha/an7581/*w1700k* 2>/dev/null || echo "No W1700K artifacts"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: w1700k-firmware
          path: |
            bin/targets/airoha/an7581/*w1700k*
            bin/targets/airoha/an7581/sha256sums
          retention-days: 30

  build-xg2010g:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && inputs.device == 'gemtek_xg2010g-ubi'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: true

      - name: Setup build environment
        run: |
          sudo apt update
          sudo apt install -y build-essential clang gcc g++ binutils bzip2 gawk gettext \
            git libncurses-dev libssl-dev python3 python3-setuptools rsync unzip \
            wget xsltproc zlib1g-dev file which perl sed make

      - name: Install feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Configure
        run: |
          if [ ! -f .config ]; then
            make defconfig
          fi
          if ! grep -q "DEVICE_gemtek_xg2010g-ubi=y" .config; then
            echo "CONFIG_TARGET_airoha_an7581_DEVICE_gemtek_xg2010g-ubi=y" >> .config
          fi
          make defconfig

      - name: Download sources
        run: make download -j$(nproc)

      - name: Build XG2010G
        run: |
          THREADS="${THREADS:-$(nproc)}"
          echo "Using $THREADS threads"
          make -j$THREADS image DEVICE=gemtek_xg2010g-ubi 2>&1 | tee build.log

      - name: Show artifacts
        run: |
          echo "=== XG2010G Firmware ==="
          ls -lh bin/targets/airoha/an7581/*xg2010g* 2>/dev/null || echo "No XG2010G artifacts"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xg2010g-firmware
          path: |
            bin/targets/airoha/an7581/*xg2010g*
            bin/targets/airoha/an7581/sha256sums
          retention-days: 30
